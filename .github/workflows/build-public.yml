# Builds the Next.js app in src/ and commits the static export to public/,
# which is what the ndif.us server serves from main. Keeps the old
# "anything in public on main is served" contract without contributors
# having to remember `make all`.
#
# Fully reversible: delete this file and the manual `make all` + commit
# workflow (README) is back to being the only path.
name: Build site into public/

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "Makefile"
      - ".github/workflows/build-public.yml"
  # Weekly refresh so the GitHub star stats baked into the homepage stay
  # current even when nobody pushes. No-op when the numbers haven't moved
  # (deterministic build id + fetch script only rewrites on change).
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: build-public
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # The bot's own commits only touch public/ (filtered out by `paths`
    # above) — this guard is belt-and-braces against loops.
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Build static export
        working-directory: src
        env:
          # Lifts the GitHub API rate limit for scripts/fetch-org-stats.mjs
          # (which degrades to the committed org-stats.json on any failure).
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bun install --frozen-lockfile
          bun run build

      - name: Publish export to public/
        run: |
          rm -rf public
          cp -R src/out public

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # org-stats.json is included so the committed fallback for the
          # star count tracks the freshly fetched value.
          git add -A public src/data/org-stats.json
          if git diff --cached --quiet; then
            echo "public/ unchanged — nothing to commit"
            exit 0
          fi
          git commit -m "build: refresh public/ for ${GITHUB_SHA}"
          # One rebase-retry in case another push landed mid-build.
          git push origin main || (git pull --rebase origin main && git push origin main)
